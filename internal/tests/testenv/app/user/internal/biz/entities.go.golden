// 该文件仅生成一次，可以在此添加自定义业务逻辑。
package biz

// Group 是业务实体，嵌入了生成的 Base 结构体。
type Group struct {
	GroupBase
}

// Post 是业务实体，嵌入了生成的 Base 结构体。
type Post struct {
	PostBase
}

// User 是业务实体，嵌入了生成的 Base 结构体。
type User struct {
	UserBase
}
